# Memory for 2026-06-20

- **Troubleshooting command**: The user ran `python train_model.py` in the `backend/data` directory which failed because the script is located in the `backend` directory.
- **Dataset File Name Issue**: The Kaggle stroke dataset was saved as `healthcare-dataset-stroke-data (1).csv` in `backend/data/`. `train_model.py` expects it to be `healthcare-dataset-stroke-data.csv`.
- **Action Taken**:
  - Copied `healthcare-dataset-stroke-data (1).csv` to `healthcare-dataset-stroke-data.csv` in `backend/data/`.
  - Updated output path in `backend/data/generate_mock_data.py` to write directly to `healthcare-dataset-stroke-data.csv` for future consistency.
- **NaN Gender Issue**: Found that `gender` column contained a single `NaN` value which was not mapped by `ENCODING_MAPS` and caused `SMOTE` to fail due to `NaN` in inputs.
  - Modified `backend/train_model.py` to filter out any rows with invalid/missing gender (not in `['Female', 'Male']`).
- **Model Training Success**: Successfully trained Logistic Regression, Decision Tree, and KNN models from the `backend/` directory. All models, the scaler, encoding maps, and feature names are saved in `backend/models/`.
